song.mp3 is already in here — "Moment 4 Life" (Nicki Minaj ft. Drake).

The site references assets/audio/song.mp3 and starts it (only after she
taps "Tap to Begin", since browsers block autoplay with sound until the
visitor interacts with the page).

To swap it for a different song, just replace this file — keep the exact
name "song.mp3" and nothing else needs to change. Keep it reasonably small
(a few MB) so it loads fast on mobile data.
